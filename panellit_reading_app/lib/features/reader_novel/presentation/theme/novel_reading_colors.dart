import 'package:flutter/material.dart';

class NovelReadingColors {
  // ── Background ─────────────────────────────────────────────────────────
  static const Color background = Color(0xFFFAF8F5);
  static const Color contentBackground = Color(0xFFFFFFFF);

  // ── Top Bar ─────────────────────────────────────────────────────────────
  static const Color topBarBackground = Color(0xFFFFFFFF);
  static const Color topBarTitle = Color(0xFF1C2333);
  static const Color topBarSubtitle = Color(0xFF8A96A3);
  static const Color topBarIcon = Color(0xFF2F3B46);

  // ── Text Content ────────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF1C2333);
  static const Color textSecondary = Color(0xFF4A5568);
  static const Color chapterDivider = Color(0xFFE2E8F0);

  // ── Bookmark ────────────────────────────────────────────────────────────
  static const Color bookmarkActive = Color(0xFFF2B705);
  static const Color bookmarkInactive = Color(0xFF2F3B46);
  static const Color bookmarkDisabled = Color(0xFFB0BEC5);

  // ── Control Bar ─────────────────────────────────────────────────────────
  static const Color controlBar = Color(0xFFFFFFFF);
  static const Color controlIcon = Color(0xFF2F3B46);
  static const Color controlInactive = Color(0xFFB0BEC5);

  // ── Magic FAB ───────────────────────────────────────────────────────────
  static const Color magicPrimary = Color(0xFF7C3AED);
  static const Color magicSecondary = Color(0xFFBB86FC);
  static const Color magicGlow = Color(0x337C3AED);
  static const Color actionText = Color(0xFF273140);
  static const Color actionShadow = Color(0x1A000000);

  // ── Sidebar ─────────────────────────────────────────────────────────────
  static const Color sidebarHandle = Color(0xFFDDE3EC);
  static const Color sidebarHandleIcon = Color(0xFF8A96A3);
  static const Color sidebarPanel = Color(0xFFFFFFFF);
  static const Color sidebarPanelShadow = Color(0x22000000);
  static const Color sidebarPanelBorder = Color(0xFFE8EDF4);

  // ── Guest Hint ──────────────────────────────────────────────────────────
  static const Color guestHint = Color(0xFF8A96A3);
}
